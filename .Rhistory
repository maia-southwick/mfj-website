install.packages("usethis")
library(usethis)
usethis::use_github()
quarto preview help
list.files(path = "../01_Data & Analytics/") # add ../ in the quotes to step back a folder
?list.files
list.files(path = "../01_Data & Analytics/") # add ../ in the quotes to step back a folder
setwd("../01_Data & Analytics/") #use the tab key to search in the file path!
dir() #use the tab key to search in the file path!
dir("") #use the tab key to search in the file path!
quarto render
if(!require("pathdiagram")) install.packages("pathdiagram")
remove(list = ls())
installed.packages()
(.packages())
install.packages("pathdiagram")
(.packages())
library(pathdiagram)
(.packages())
